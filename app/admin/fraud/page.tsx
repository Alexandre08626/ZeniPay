import { AdminStub } from "../_lib/StubPage";
export default function P() { return <AdminStub title="Fraud" blurb="Fraud alerts + anomaly signals across all orgs. Severity drill-down, decline/escalate actions." />; }
