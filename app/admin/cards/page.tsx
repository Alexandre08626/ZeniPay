import { AdminStub } from "../_lib/StubPage";
export default function P() { return <AdminStub title="Cards" blurb="Every issued card across merchant + agent + personal surfaces, with provider state and freeze/cancel controls." />; }
