import { AdminStub } from "../_lib/StubPage";
export default function P() { return <AdminStub title="Compliance" blurb="SOC2 controls + KYB queue + audit log feed + compliance dashboard rollup." />; }
