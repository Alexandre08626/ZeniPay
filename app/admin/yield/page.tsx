import { AdminStub } from "../_lib/StubPage";
export default function P() { return <AdminStub title="Yield" blurb="Config (rate / split / min balance), enrollments table, daily accruals, payouts, platform revenue chart, manual cron triggers." />; }
